defaults = {
    'lockbox': {
        'enabled': False,
        'port': 8091,
        'version': 'latest',  # per-node override pins the real version — https://hub.docker.com/r/bborbe/lockbox/tags
    },
}
